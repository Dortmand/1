name: my_new_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^9.9.1
  google_ml_kit: ^0.7.3
  http: ^1.2.2
  image_picker: ^0.8.9
  leak_tracker: ^10.0.5
  flutter_local_notifications_linux: ^0.5.1 # Для поддержки Linux
  flutter_local_notifications_platform_interface: ^5.0.0 # Обеспечение совместимости
  image_picker_android: ^0.8.12+14 # Для поддержки Android
  image_picker_ios: ^0.8.12 # Для поддержки iOS

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
