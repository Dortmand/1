buildscript {
    ext.kotlin_version = '1.7.10' // Замените на последнюю стабильную версию
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.2.2' // Замените на последнюю стабильную версию
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}
